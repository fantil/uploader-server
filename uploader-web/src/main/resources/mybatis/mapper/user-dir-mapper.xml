<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.fanti.uploader.server.mapper.UserDirMapper">
    <select id="getUserDirListWithFullPath" resultType="org.fanti.uploader.server.db.UserDir">
        SELECT
            d.id id,
            d.user_id userId,
            d.parent_id parentId,
            d.dir_name dirName,
            d.full_path fullPath,
            d.create_time createTime,
            d.modify_time modifyTime
        FROM
            user_dir d
        WHERE
            d.user_id=#{userId}
        AND
            d.full_path=#{fullPath}
    </select>

    <select id="getUserDirList" resultType="org.fanti.uploader.server.db.UserDir">
        SELECT
            d.id id,
            d.user_id userId,
            d.parent_id parentId,
            d.dir_name dirName,
            d.full_path fullPath,
            d.create_time createTime,
            d.modify_time modifyTime
        FROM
            user_dir d
        WHERE
            d.user_id=#{userId}
    </select>
</mapper>